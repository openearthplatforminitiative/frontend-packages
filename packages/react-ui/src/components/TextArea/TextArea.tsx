"use client"

import { Field as ArkField } from "@ark-ui/react"
import { textArea } from "@openepi/styled-system/recipes"
import { styled } from "@openepi/styled-system/jsx"

export const TextArea = styled(ArkField.Textarea, textArea)
